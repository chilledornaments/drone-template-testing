kind: template
load: example_template.yaml
data:
  trigger:
    events:
      - push
    branches:
      - testing
      - main
  pipeline_name: hello world
  hello_world_commands:
    - ls
    - whoami
  test_if:
    run: true

  top_level:
    - name: dev
      greeting: "hello world"


